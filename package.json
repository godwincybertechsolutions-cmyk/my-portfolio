{
  "name": "godwin-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^2.12.7"
  },
  "scripts": {
    "build": "mkdir -p public && cp -r *.html *.js *.css *.json assets/ public/ 2>/dev/null || true && cp -r components/ public/components/ 2>/dev/null || true && echo 'Build completed!'",
    "start": "echo 'Static site - no server required'"
  }
}
